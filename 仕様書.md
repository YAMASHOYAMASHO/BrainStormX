# 製品仕様書 (PRD): Thinker
**Version:** 1.0.0
**Status:** Draft
**Platform:** Mobile (iOS/Android)
**Tech Stack:** Flutter, Firebase (Auth, Firestore, Functions, Storage, Analytics), Firebase AI Logic

---

## 1. プロジェクト概要

### 1.1 プロダクト定義
**「Thinker」**は、自分だけの非公開タイムライン上で、多様なAIエージェント（ペルソナ）から多角的なフィードバックを受け、アイデアを高速に推敲・深化させる**思考支援プラットフォーム**である。

### 1.2 解決する課題
* **検証効率の低さ:** 公開SNSでは反応が遅く、また未完成・機密性の高いアイデアは投稿できない。
* **AIの同質化:** 1対1のチャットでは意見が平均化（要約）され、尖った指摘が得られにくい。
* **思考の断片化:** アイデアの着想から深化までのプロセスが、日々のタスクにより中断・消失してしまう。

### 1.3 コアバリュー (Value Proposition)
* **心理的安全性:** 誰にも見られない「壁打ちサンドボックス」。
* **多様性:** 100種類以上の尖ったペルソナによる「擬似社会」の再現。
* **文脈保持:** 「リツイート」機能による、思考プロセスのセーブポイント化。

---

## 2. システムアーキテクチャ構成

### 2.1 全体構成
* **Frontend:** Flutter (Riverpod/Provider 推奨)
* **Backend:** Firebase Serverless Architecture
* **AI Model:** Google Gemini 1.5 Flash (or 2.0 Flash)
    * *選定理由:* 大量のペルソナを一括生成するためのContext Windowの広さと、低レイテンシ・低コスト。

### 2.2 データフロー
1.  **User:** Flutterアプリからテキスト/画像を投稿。
2.  **Firestore:** `posts` コレクションに書き込み (`onCreate` トリガー)。
3.  **Cloud Functions:**
    * ペルソナプールからランダムに30名を抽出。
    * ユーザー投稿とペルソナリストをGemini APIに送信（Batch Request）。
    * GeminiからのJSONレスポンスを解析。
    * Firestoreの `responses` サブコレクションに一括書き込み。
4.  **Flutter:** `StreamBuilder` で `responses` をリアルタイム検知し、タイムラインにアニメーション付きで流し込む。

---

## 3. 機能要件 (Functional Requirements)

### 3.1 認証・プロジェクト管理
* **ユーザー認証:** Firebase Auth (Google Sign-In, Email/Password, Anonymous)。
* **プロジェクト（スレッド）管理:**
    * テーマごとにプロジェクトを作成（例：「修論」「新規事業」）。
    * プロジェクト一覧表示、追加、削除。

### 3.2 仮想タイムライン（推敲サンドボックス）
* **投稿機能:**
    * テキスト入力。
    * 画像添付 (Firebase Storage)。
* **AIレスポンス表示 (Feed UI):**
    * **返信 (Reply):** ユーザーへの直接的なメンション形式。
    * **引用 (Quote):** 第三者視点の独り言形式（「これ〇〇に使えそうじゃね？」）。
    * **UI演出:** 全ての意見が一斉に出るのではなく、ストリーミングまたは少しの遅延を持って「タイムラインが盛り上がっている」様子を演出する。
* **思考のストック (Retweet):**
    * ユーザーが「有益だ」と感じたAIのレスポンスを「リツイート」ボタンで保存。
    * リツイートされた内容は、思考の文脈（Context）として次回のAI生成プロンプトに含まれる。

### 3.3 マルチ・ペルソナ・エンジン
* **ペルソナ定義:** 約100種類のSystem Prompt定義を持つ。
    * *属性:* 名前、職業、年齢、性格（肯定/否定/分析）、口調、関心領域。
* **ランダム選抜ロジック:**
    * 投稿ごとに約30名を動的に選出。
    * パラメータ制御: 肯定:否定:分析 = 2:2:6 などの比率を維持しつつランダム化。

### 3.4 思考深化（ドリルダウン）
* **深掘り機能:**
    * 特定のAIレスポンスに対して「返信」することで、そのAI（＋関連する野次馬AI）とのサブスレッドを開始。
    * 議論の深さを管理する。

---

## 4. データモデル (Firestore Schema Plan)

NoSQL設計として、読み取りコストとリアルタイム性を最適化する。

### `users` (Collection)
* `uid` (Doc ID)
    * `isPremium`: boolean (課金フラグ)
    * `createdAt`: timestamp

### `projects` (Sub-Collection under users)
* `users/{uid}/projects/{projectId}`
    * `title`: string ("修論スレ" etc.)
    * `lastActivity`: timestamp
    * `contextSummary`: string (これまでの議論の要約、AI用コンテキスト)

### `posts` (Sub-Collection under projects)
* `users/{uid}/projects/{projectId}/posts/{postId}`
    * `content`: string
    * `imageUrl`: string (Optional)
    * `createdAt`: timestamp
    * `targetResponseId`: string (nullなら新規投稿、IDありなら深掘り返信)

### `responses` (Sub-Collection under posts)
* `users/{uid}/projects/{projectId}/posts/{postId}/responses/{responseId}`
    * `personaId`: string ("persona_001")
    * `personaName`: string ("辛口投資家")
    * `type`: string ("REPLY" | "QUOTE")
    * `content`: string
    * `sentiment`: string ("POSITIVE" | "NEGATIVE" | "NEUTRAL")
    * `isRetweeted`: boolean (ユーザーが採用したか)
    * `createdAt`: timestamp

### `personas` (Root Collection - Master Data)
* `personas/{personaId}`
    * `name`: string
    * `systemPrompt`: string
    * `category`: string

---

## 5. 技術実装詳細

### 5.1 AI Prompting Strategy (Batching)
コストと速度の観点から、1リクエストで複数人の人格を演じさせる。

**System Prompt Example:**
```text
あなたは仮想SNSのシミュレータです。以下のユーザーの投稿に対し、指定された複数のペルソナになりきってJSON形式で反応を出力してください。

【制約】
- 出力フォーマット: JSON Array
- response_mime_type: application/json
- 各ペルソナの口調と視点を厳密に守ること。

【参加ペルソナリスト】
1. {name: "女子大生", stance: "直感的・ミーハー", prompt: "..."}
2. {name: "老舗旅館の女将", stance: "保守的・丁寧", prompt: "..."}
... (10名分)

【ユーザー投稿】
{user_content}
```

### 5.2 Flutter UI/UX
* **State Management:** Riverpod
* **Router:** GoRouter
* **Animations:** `flutter_animate` パッケージを使用し、リストアイテムが下からフワッと現れる演出を入れる。

---

## 6. マネタイズ & 制限事項

### 6.1 Freemium Model
**Free Plan:**
* プロジェクト数: 最大3つ
* 1投稿あたりのAI応答数: 最大10件
* 深掘り機能: 広告視聴後にアンロック

**Premium Plan (サブスクリプション):**
* プロジェクト数: 無制限
* 1投稿あたりのAI応答数: 最大50件 (お祭り状態)
* カスタムペルソナ作成機能
* 要約エクスポート (Markdown/PDF)

### 6.2 ガードレール (安全性)
**Gemini Safety Settings:**
* Hate Speech / Harassment: `BLOCK_MEDIUM_AND_ABOVE`
* ただし、「辛口意見」は許容する必要があるため、プロンプト内で「これはフィクションのブレインストーミングであり、批判的な意見も歓迎される」と定義し、過剰な検閲を回避する。

---

## 7. TODO & ロードマップ

### Phase 1: MVP (ハッカソン/プロトタイプ)
- [x] Firebase Project Setup（thinker-ai-app）
- [x] Auth & Firestore 基本実装
- [x] ペルソナデータの作成（30種類用意）
- [x] Firebase AI Logic (Gemini) 連携
- [x] Flutter タイムラインUIの実装

### Phase 2: ブラッシュアップ
- [ ] 引用リプライ(Quote)と通常リプライのUI差別化
- [ ] 「リツイート（コンテキスト保存）」ロジックの実装
- [ ] アニメーション演出の強化

### Phase 3: リリース準備
- [ ] 課金実装 (RevenueCat推奨)
- [ ] AdMob実装
- [ ] 利用規約・プライバシーポリシー作成